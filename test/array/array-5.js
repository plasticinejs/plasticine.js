import data from './data';

export default {
    data,
    template : {
        root : '$.undefined.undefined'
    },
    result : [{
        root : undefined
    }, {
        root : undefined
    }]
}